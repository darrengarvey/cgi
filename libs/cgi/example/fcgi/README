Note that at the moment linux is probably the only platform that FastCGI examples will work on (behind apache or apache2 with mod_fastcgi/mod_fcgid).

This situation is temporary, but for now I'm not sure how much.
